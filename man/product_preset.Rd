% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{product_preset}
\alias{product_preset}
\alias{product_presets}
\title{Use the current example product names}
\usage{
## Call signatures
product_preset(name)

product_presets()
}
\arguments{
\item{name}{One required character name, \code{'pnw'}, \code{'us_south'}, or \code{'douglas_fir'},
unitless. No default. Missing or unknown names are errors.

Example: \code{'pnw'}.}
}
\value{
The product table described in \link[=product_schema]{Product specification schema}, or the
example-name
table described by \code{\link[=example_product_names]{example_product_names()}}.
}
\description{
Use \code{\link[=example_products]{example_products()}} to load a shipped product table and
\code{\link[=example_product_names]{example_product_names()}} to list available tables. These older names call
the replacements and return the same objects. Update new analyses to use the
current names.
}
\details{
A deprecation message names the replacement once per session.
}
\section{Status and missing values}{

These aliases add no status codes. Invalid names stop the call. Missing
optional product bounds retain the meanings in \link[=product_schema]{Product specification schema}.
}

\examples{
## Load dplyr
library(dplyr)

## Inspect the example product measurements
example_products(name = 'pnw') \%>\%
  select(product, priority, volume_unit, inside_bark)
}
\seealso{
\code{\link[=example_products]{example_products()}} for current loading and \code{\link[=example_product_names]{example_product_names()}}
for the available names and geographic defaults.
}
\keyword{internal}
