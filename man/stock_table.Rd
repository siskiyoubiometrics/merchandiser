% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stand-table.R
\name{stock_table}
\alias{stock_table}
\title{Report product stock by diameter class}
\usage{
## Call signatures
stock_table(x, trees, expansion, by = c('product', 'dbh_class'), class_width = 2, area_unit =
  'acre', na_action = 'warn')
}
\arguments{
\item{x}{One result from \code{\link[=merchandise]{merchandise()}} or \code{\link[=optimize_bucking]{optimize_bucking()}}. Required. Missing is an error.

\code{trees$id} and \code{logs$id} identify trees. Log fields
\code{product} (unitless label), \code{net_scale} (net quantity), and \code{scale_unit}
(quantity unit) provide volume. Other result fields are unchanged.

Use \code{status = TRUE} when creating the result.}

\item{trees}{Required data frame with one row per caller tree. \code{id} must
contain unique, nonmissing identifiers matching every result tree. Additional caller trees are
retained as \code{not_in_result}.

\code{dbh} is numeric
diameter at breast height outside bark, in inches for an imperial
result or centimeters for a metric result. Missing, nonfinite, or
nonpositive diameters retain tree counts but make their group's basal
area missing. \code{species} supplies grouping labels, or \code{spcd} supplies
species codes if \code{species} is absent.

These are unitless. Other columns
requested in \code{by} must be atomic grouping labels. Missing group labels
form their own group.

Example: \code{example_trees}. Omission is an error.}

\item{expansion}{Required numeric vector of finite, nonnegative trees per
area represented by each record. Length one is recycled, otherwise
length must equal \code{nrow(trees)} in caller row order, not result order. Missing values are
errors.

Example: \code{expansion = 5} for trees per acre
when \code{area_unit = 'acre'}. No expansion is inferred from the inventory.}

\item{by}{Character vector of distinct grouping names, default
\code{c('product', 'dbh_class')}. Unitless. Example: \code{by = 'product'}.

Missing, unknown, or output names are errors. See \code{\link[=stand_table]{stand_table()}} for grouping
definitions and the behavior of \code{character()}.}

\item{class_width}{One finite positive numeric diameter class width. Default \code{2}, in inches for
imperial results or centimeters for metric
results. Classes are left-closed intervals with lower boundary
\code{floor(dbh / class_width) * class_width}, reported in \code{dbh_class}.

Missing values are errors.}

\item{area_unit}{One character label, \code{'acre'} (default) or \code{'hectare'}. Labels the supplied
expansion, it does not convert expansion factors. Missing or unknown values are errors.

Example: \code{area_unit = 'hectare'}.}

\item{na_action}{One character value, \code{'warn'} (default) or \code{'propagate'}. Both propagate
missing quantities. \code{'warn'} emits one consolidated
warning for entirely missing quantities.

Explicit \code{'propagate'} stays
silent. Missing or unknown values are errors. Unitless.

Example: \code{na_action = 'propagate'}.}
}
\value{
The \code{\link[=stand_table]{stand_table()}} data frame contains weighted tree counts, basal area, log counts,
and net scaled quantities. It separates volume statuses and retains a \code{failed_trees} attribute.
}
\description{
Expand an inventory with product and diameter-class grouping by default. Return the same
fields and status groups as \code{\link[=stand_table]{stand_table()}}.
}
\section{Status and missing values}{

Failures are separate from valid volume. Trees without logs remain in
counts. Failed and absent trees have missing volume quantities.

See
\code{\link[=stand_table]{stand_table()}} for the complete field and status definitions. One consolidated warning names
each entirely missing quantity and its
group. Explicit \code{na_action = 'propagate'} suppresses that warning.

Partially missing quantities still propagate under either choice.
}

\examples{
## Load dplyr
library(dplyr)

## Compile the example inventory with supplied expansion
stock_table(x = merchandise(dbh = example_trees$dbh,
                            ht = example_trees$ht,
                            species = example_trees$species,
                            products = example_products(name = 'pnw'),
                            status = TRUE),
            trees = example_trees \%>\%
            mutate(id = tree),
            expansion = 5,
            by = 'species')
}
\seealso{
\code{\link[=stand_table]{stand_table()}} for tree density and basal area,
\code{\link[=product_summary]{product_summary()}} for totals without the complete-inventory join.
}
