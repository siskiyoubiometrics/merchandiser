% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem-nsvb.R
\name{nvel_rules}
\alias{nvel_rules}
\title{Record source-library log-length and measurement rules}
\source{
United States Forest Service source library \code{mrules.f}, commit
\verb{38548071d5aa652bb90c7f111f86b427f798a1c9}.
}
\usage{
## Call signatures
nvel_rules(even_or_odd = NA_integer_, option = NA_integer_, maximum_length = NA_real_,
  minimum_length = NA_real_, minimum_top_length = NA_real_, merchantable_length = NA_real_,
  primary_top = NA_real_, secondary_top = NA_real_, stump = NA_real_, trim = NA_real_,
  bark_ratio = NA_real_, minimum_board_foot_dbh = NA_real_, scribner = 'regional', prod =
  '01', ht_type = '', live = 'L', ctype = 'C', cull = 0, forest = 0, district = 0)
}
\arguments{
\item{even_or_odd}{National Volume Estimator Library \code{EVOD} segment-length code
(unitless): \code{1} permits
odd nominal lengths and \code{2} permits only even nominal lengths. Options 11
through 14 define their own fixed-length behavior. \code{NA} requests the
library default.

Numeric nonempty vector. Accepted finite values are 1 or 2. Default \code{NA} applies when omitted.

Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{option}{source library \code{OPT} segmentation code (unitless). Codes 11, 12, 13,
and 14 use the library's 16-, 20-, 32-, and 40-foot scale conventions. Codes 21 through 24
select the four nominal-log top-segment rules.

\code{NA}
requests the library default. Numeric nonempty vector. Accepted finite values are 11 through
14 or 21 through 24.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{maximum_length}{source library \code{MAXLEN}, the maximum nominal segment length, in
the consuming call's height units. Numeric nonempty vector. Accepted finite values are greater
than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{minimum_length}{source library \code{MINLEN}, the minimum nominal segment length, in
the consuming call's height units. Numeric nonempty vector. Accepted finite values are greater
than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{minimum_top_length}{source library \code{MINLENT}, the minimum accepted top-log
length, in the consuming call's height units. Numeric nonempty vector. Accepted finite values
are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{merchantable_length}{source library \code{MERCHL}, the minimum length of primary
product required for the tree to be merchantable, in the consuming call's
height units. Numeric nonempty vector. Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{primary_top}{source library \code{MTOPP}, the primary-product top diameter inside
bark used for board-foot, cubic, and cord volume, in the consuming call's
diameter units. Numeric nonempty vector. Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{secondary_top}{source library \code{MTOPS}, the secondary-product top diameter inside
bark used for cubic and cord volume, in the consuming call's diameter
units. Numeric nonempty vector. Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{stump}{source library \code{STUMP}, the groundline-to-stump height, in the consuming
call's height units. Numeric nonempty vector. Accepted finite values are at least zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{trim}{source library \code{TRIM}, the trim allowance added to each segment, in the
consuming call's height units. Numeric nonempty vector. Accepted finite values are greater
than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{bark_ratio}{The source library bark-ratio auxiliary represented by \code{merchandiser}
as the unitless constant diameter-inside-bark to diameter-outside-bark
ratio in \verb{(0, 1]}. \code{NA} leaves bark to the selected model or library
default. Numeric nonempty vector.

Accepted finite values are greater than zero and at most one. Default \code{NA} applies when omitted.
Numeric \code{NA} leaves a source
default unset.

Infinite values are errors.}

\item{minimum_board_foot_dbh}{source library \code{MINBFD}, the minimum outside-bark diameter
at breast height
for board-foot eligibility, in the consuming call's diameter units. Numeric nonempty vector.
Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{scribner}{source library \code{COR} Scribner-volume mode (unitless): \code{'table'} uses
the Scribner table, \code{'factor'} uses factors, and \code{'regional'} requests
the regional library default. Nonempty character vector, default \code{'regional'} when omitted.
Missing elements and unknown labels are errors.

Example: \code{scribner = 'regional'}.}

\item{prod}{source library two-character product code (unitless), such as \code{'01'} for
sawtimber or \code{'08'} for a nonsaw product.
Nonempty character vector, default \code{'01'} when omitted.
Missing elements and unknown labels are errors.}

\item{ht_type}{source library \code{HTTYPE} code (unitless): \code{'F'} denotes a physical
height, \code{'L'} denotes a height expressed in logs, and \code{''} leaves the
field unspecified for the consuming call. Nonempty character vector, default \code{''} when
omitted. Missing elements and unknown labels are errors.

Example: \code{ht_type = ''}.}

\item{live}{source library live/dead status code (unitless): \code{'L'} for live or \code{'D'}
for dead. Nonempty character vector, default \code{'L'} when omitted. Missing elements and unknown
labels are errors.

Example: \code{live = 'L'}.}

\item{ctype}{Source call-origin code, unitless. \code{'C'} selects cruise, \code{'I'} selects Forest
Inventory and Analysis, and \code{'F'} selects Forest Vegetation Simulator. \code{'B'} selects the
alternate National Scale Volume and Biomass route.

Nonempty character vector, default \code{'C'} when omitted. Missing elements and unknown labels are
errors. Example: \code{ctype = 'C'}.}

\item{cull}{source library \code{CULL}, the whole-tree cull deduction as a percentage from
0 through 100. Numeric nonempty vector. Accepted finite values are zero through 100.

Default \code{0} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.}

\item{forest}{source library two-digit national-forest code represented as an integer
from 0 through 99 (unitless). Zero means no forest-specific selection. Numeric nonempty
vector.

Accepted finite values are whole numbers from zero through 99. Default \code{0} applies when omitted.
Numeric \code{NA} leaves a source
default unset.

Infinite values are errors.}

\item{district}{source library two-digit ranger-district code represented as an
integer from 0 through 99 (unitless). Zero means no district-specific
selection. Numeric nonempty vector.

Accepted finite values are whole numbers from zero through 99. Default \code{0} applies when omitted.
Numeric \code{NA} leaves a source
default unset.

Infinite values are errors.}
}
\value{
A validated list with class \code{treevolume_nvel_rules}. Input vectors
are preserved for common-size recycling by the consuming calculation.
}
\description{
Validate a record of source-library segmentation and measurement settings.
}
\details{
Creates and validates the merchant-rule record used by the pinned National
Volume Estimator Library \code{MERCHRULES} interface. Numeric \code{NA} values
request the applicable library default when a model, region, product, and
tree are later supplied. Lengths and diameters use the units of the
consuming call: feet and inches for imperial calls, or meters and
centimeters for metric calls.

This record carries source defaults and supplied overrides without
calculating logs. Use \code{\link[=products_from_nvel_rules]{products_from_nvel_rules()}} to translate one
complete record into product and utilization settings. Use \code{\link[=product]{product()}}
to enter buyer specifications directly.
}
\section{Record lengths and missing values}{

This function preserves each argument vector and does not check that all vector lengths
match. A later consumer repeats length-one values and requires other lengths to match. The
product translator requires exactly one value in every field.

An unset numeric value is not a usable default for that translator's required merchant
fields. Complete them first.
}

\section{Status and missing values}{

No tree codes are returned.
Invalid fields stop construction.

Missing numeric fields remain missing and do not mean zero. No volume is calculated until
the record is translated and used in a tree calculation.
}

\examples{
## Load dplyr
library(dplyr)

## Select source lengths from an example product
settings <- example_products(name = 'douglas_fir')

## Store the source-rule override
rules <- nvel_rules(maximum_length = first(x = settings$lengths), trim = settings$trim)

## Inspect the saved length override
rules$maximum_length
}
\seealso{
\code{\link[=products_from_nvel_rules]{products_from_nvel_rules()}} to translate a complete record, \code{\link[=product]{product()}} to specify products
directly, \code{\link[=nvel_source_revision]{nvel_source_revision()}} for the source version used by the installed package.

Other source library identifiers and defaults:
\code{\link[=nvel_from_fia_code]{nvel_from_fia_code()}}
}
\concept{source library identifiers and defaults}
