% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem-defaults.R
\name{nvel_from_fia_code}
\alias{nvel_from_fia_code}
\title{Translate inventory equation codes to source equation names}
\source{
United States Forest Service source library, commit
\verb{38548071d5aa652bb90c7f111f86b427f798a1c9}.
}
\usage{
## Call signatures
nvel_from_fia_code(fia_code, spcd, geosub = '', primary_top = 0)
}
\arguments{
\item{fia_code}{Required character vector of inventory equation codes, length one
or one per input. Surrounding spaces are removed,
letters are made uppercase, and the first eight characters are used. Missing or unrecognized
codes give source error flag 1.

Unitless.}

\item{spcd}{Required numeric whole-number inventory species code from 1 through
9999, length one or one per input. Unitless. Missing
gives source flag 1.

Invalid finite codes stop the call.}

\item{geosub}{Character geographic subregion, length one or one per input. Default \code{''} uses the
source's unqualified substitution. Missing gives
flag 1.

Unitless. Geographic substitutions
depend on the selected equation template.}

\item{primary_top}{Numeric nonnegative finite primary-product top diameter in inches,
length one or one per input. Default \code{0} permits the source volume type
to set its diameter convention. Missing gives flag 1.

Values at least
99 preserve the source template and volume type without substitutions. There is no metric unit
argument.}
}
\value{
A data frame containing \code{model}, \code{volume_type}, \code{primary_top}, and
the National Volume Estimator Library-compatible \code{errflag}.
}
\description{
Translate Forest Inventory and Analysis inventory equation codes to source-library
identifiers.
}
\details{
Looks up the tree inventory board-foot (\code{BDLIST}) and cubic (\code{CULIST}) crosswalks and
applies the source routine's species, geographic, and top-diameter
substitutions.
With \code{options(merchandiser.compat = 'nvel')}, fixed-width source semantics
leave the intended one- and two-character geographic branches unreachable.
The default \code{'port'} mode applies those stated substitutions.
}
\section{Returned crosswalk fields}{

Character \code{model} is the selected equation identifier and \code{volume_type} is the source
measurement label. Both are unitless and missing on failure. Numeric \code{primary_top} is the
resolved source top diameter in inches, retained even when a crosswalk fails.

Integer \code{errflag} is a source error flag, not the package's tree-status numbering. No
rounding is applied to the returned diameter beyond source volume-type substitutions.
}

\section{Status and missing values}{

Flag 0 means the crosswalk succeeded. Flag 1 means
missing required values or an unmatched code.

Flag 6 means a species cannot fit the source's three-digit species template. Correct flags 1
and 6 before using the identifier. A successful crosswalk does not establish equation
availability.
}

\examples{
## Translate a source inventory equation code
nvel_from_fia_code(fia_code = 'BD000006', spcd = 122, primary_top = 6)
}
\seealso{
\code{\link[=has_taper_model]{has_taper_model()}} to check availability, \code{\link[=nvel_default_equation]{nvel_default_equation()}}
to select by geography, \code{\link[=nvel_rules]{nvel_rules()}} to specify source measurement rules.

Other source library identifiers and defaults:
\code{\link[=nvel_rules]{nvel_rules()}}
}
\concept{source library identifiers and defaults}
