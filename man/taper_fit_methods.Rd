% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem-taper_fit.R
\name{taper_fit_methods}
\alias{taper_fit_methods}
\alias{print.taper_fit}
\alias{summary.taper_fit}
\alias{print.summary.taper_fit}
\alias{predict.taper_fit}
\alias{plot.taper_fit}
\title{Inspect fitted diameters and taper residuals}
\usage{
## Predict diameters from a taper fit
\method{predict}{taper_fit}(object, newdata = NULL, re_form = c('conditional', 'population'),
  group = NULL, units = object$input_units, ...)

## Draw retained measurements and fitted diameters
\method{plot}{taper_fit}(x, ...)
}
\arguments{
\item{x, object}{Required complete \code{taper_fit} from \code{\link[=fit_taper]{fit_taper()}}. No default. Missing or other
objects are errors.

Example: \code{fit}.}

\item{...}{Named graphical settings for the plot method, for example
\code{cex = 0.7}. Omission uses its default points and axes.

Plot coordinates are
relative height and relative diameter, both unitless. Missing graphical
settings follow base graphics behavior. The other methods ignore dots.}

\item{newdata}{Data frame with one row per predicted diameter, default \code{NULL}
to use retained fitting measurements. Required numeric columns are positive
\code{dbh} and \code{ht}, and \code{h} from zero through \code{ht}. Height aliases accepted by
\code{\link[=fit_taper]{fit_taper()}} also work.

Missing or invalid rows are errors. Kozak 2002
requires total height above 1.3 meters. Units follow \code{units}.}

\item{re_form}{One character value, \code{'conditional'} (default) to include known
group differences, or \code{'population'} for the common relationship. Unitless. Missing or unknown
choices are errors.

Example: \code{'population'}.}

\item{group}{Atomic group labels, one to repeat or one per prediction, or a
column name such as \code{'stand'}. Default \code{NULL} uses the retained or new-data
group column when available. Missing or unknown groups use the common
fitted relationship.

Labels are unitless.}

\item{units}{One character value, \code{'imperial'} or \code{'metric'}, defaulting to the
fit's original input units. Imperial uses inches and feet, metric centimeters
and meters. With \code{newdata = NULL}, only output diameter units change.

Missing or unknown choices are errors. Example: \code{units = 'metric'}.}
}
\value{
\code{predict()} returns a numeric diameter vector in inches or centimeters. \code{summary()}
returns
a \code{summary.taper_fit} object. The print and plot methods return \code{x}
invisibly.
}
\description{
Predict diameters, summarize fit statistics, or draw fitted diameters and residuals from a
taper fit.
}
\details{
Predictions use the common fitted relationship plus group differences
when requested and available. Plots compare observed and fitted relative
diameters across relative height. They use retained measurements, not new
field observations.
}
\section{Status and missing values}{

No status codes are added. Missing or invalid new diameter and height
measurements stop prediction. With \code{newdata = NULL}, retained fitting
measurements are used.

Unknown or missing groups use the common fitted
relationship. Plotting displays retained fitting measurements only.
}

\section{Measurement table fields}{

\code{tree_id} is a required tree label repeated for measurements on one stem. It is unitless,
for example \code{'example-1'}. \code{tree} is an accepted alias.

Required numeric \code{dbh} is positive outside-bark breast-height diameter, and \code{ht} is positive
total height above ground. \code{total_height} is an alias for \code{ht}. Required numeric \code{h} is
measurement height above ground, from zero through \code{ht}.

Its alias is \code{measurement_height}. Required numeric \code{dib} is nonnegative inside-bark
diameter at that height, with alias \code{diameter_inside_bark}. Diameters use inches or
centimeters, heights use feet or meters.

No field has a default. Missing and nonfinite measurements are omitted with a warning. If
both a preferred name and its alias occur, the preferred name is used.

Other columns matter only when named in \code{species}, \code{group}, or \code{weights}. No list columns
are required.
}

\section{Fitted taper fields}{

\code{coefficients} is a named numeric vector in the form's required order. \code{form}, \code{method},
\code{units}, \code{input_units}, and \code{package_version} are character labels. \code{n_trees} and
\code{n_measurements} are counts.

\code{species} is an integer scope vector and \code{groups_seen} is a character label vector.
\code{convergence} records logical \code{converged}, counts \code{nls_attempts} and \code{nlme_attempts},
logical \code{nls_optimizer_converged}, and character \code{nls_stop_message}. These record numerical
completion, not model validity.

\code{fit_statistics$overall} has sample count \code{n}, root mean squared error \code{rmse}, and mean error
\code{bias}. The count is unitless, while both
error measures use centimeters. \code{fit_statistics$by_relative_height} has the same fields plus
character \code{relative_height_class}, in tenths of height.

Classes include the lower boundary and exclude the upper boundary, except the final class
also contains the tip. Bias is observed minus fitted.

\code{residual_diagnostics} has \code{tree_id}, numeric \code{dbh}, \code{ht}, \code{h}, \code{relative_height},
\code{observed_dib}, \code{fitted_dib}, \code{population_fitted_dib}, \code{residual}, and
\code{standardized_residual}. Diameter and residual fields use centimeters, heights use meters,
and relative height and standardized residuals are unitless. \code{species} and \code{group} appear
when supplied.

\code{data_metric} retains fitting fields in centimeters and meters, with numeric \code{weight} and
optional \code{species}, \code{group}, and \code{p_fixed}. \code{random_effects} contains character \code{group} and
numeric \code{effect} for group differences, or zero rows. \code{random_parameter} names the adjusted
coefficient or is \code{NULL}.

\code{model} holds the fitted R model and \code{internal_coefficients} holds its transformed parameter
vector. None is a data-frame list column.
}

\examples{
## Fit the shipped stem measurements
fit <- fit_taper(data = example_stem_measurements,
                 form = 'max_burkhart',
                 units = 'imperial')

## Draw fitted diameters and residuals
plot(fit)
}
\seealso{
\code{\link[=fit_taper]{fit_taper()}} to estimate the relationship, \code{\link[=as_taper_model]{as_taper_model()}}
to use its common coefficients in volume calculations.
}
