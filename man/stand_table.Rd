% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stand-table.R
\name{stand_table}
\alias{stand_table}
\title{Expand an inventory into a stand table}
\usage{
## Call signatures
stand_table(x, trees, expansion, by = c('product', 'species', 'dbh_class'), class_width = 2,
  area_unit = 'acre', na_action = 'warn')
}
\arguments{
\item{x}{One result from \code{\link[=merchandise]{merchandise()}} or \code{\link[=optimize_bucking]{optimize_bucking()}}. Required. Missing is an error.

\code{trees$id} and \code{logs$id} identify trees. Log fields
\code{product} (unitless label), \code{net_scale} (net quantity), and \code{scale_unit}
(quantity unit) provide volume. Other result fields are unchanged.

Use \code{status = TRUE} when creating the result.}

\item{trees}{Required data frame with one row per caller tree. \code{id} must
contain unique, nonmissing identifiers matching every result tree. Additional caller trees are
retained as \code{not_in_result}.

\code{dbh} is numeric
diameter at breast height outside bark, in inches for an imperial
result or centimeters for a metric result. Missing, nonfinite, or
nonpositive diameters retain tree counts but make their group's basal
area missing. \code{species} supplies grouping labels, or \code{spcd} supplies
species codes if \code{species} is absent.

These are unitless. Other columns
requested in \code{by} must be atomic grouping labels. Missing group labels
form their own group.

Example: \code{example_trees}. Omission is an error.}

\item{expansion}{Required numeric vector of finite, nonnegative trees per
area represented by each record. Length one is recycled, otherwise
length must equal \code{nrow(trees)} in caller row order, not result order. Missing values are
errors.

Example: \code{expansion = 5} for trees per acre
when \code{area_unit = 'acre'}. No expansion is inferred from the inventory.}

\item{by}{Character vector of unique grouping column names. Default
\code{c('product', 'species', 'dbh_class')}. \code{product} comes from the logs,
\code{species} from the caller inventory, and \code{dbh_class} is calculated.

Other names refer to caller columns. \code{character()} gives totals by
volume status. Missing, unknown, or output column names are errors.

Example: \code{by = c('species', 'dbh_class')}.}

\item{class_width}{One finite positive numeric diameter class width. Default \code{2}, in inches for
imperial results or centimeters for metric
results. Classes are left-closed intervals with lower boundary
\code{floor(dbh / class_width) * class_width}, reported in \code{dbh_class}.

Missing values are errors.}

\item{area_unit}{One character label, \code{'acre'} (default) or \code{'hectare'}. Labels the supplied
expansion, it does not convert expansion factors. Missing or unknown values are errors.

Example: \code{area_unit = 'hectare'}.}

\item{na_action}{One character value, \code{'warn'} (default) or \code{'propagate'}. Both propagate
missing quantities. \code{'warn'} emits one consolidated
warning for entirely missing quantities.

Explicit \code{'propagate'} stays
silent. Missing or unknown values are errors. Unitless.

Example: \code{na_action = 'propagate'}.}
}
\value{
A data frame with the requested grouping columns and
\code{merchandising_status}, plus numeric weighted sums. \code{trees_per_acre} and
\code{failed_trees_per_acre} count all and failed trees respectively. \code{basal_area_ft2_per_acre}
uses square feet for imperial input,
\code{basal_area_m2_per_acre} uses square meters for metric input.

\code{logs_per_acre} counts logs. \verb{net_scale_<unit>_per_acre} holds net scale
quantities in each result measurement unit, for example \code{net_scale_ft3_per_acre}. With
\code{area_unit = 'hectare'}, every suffix is \verb{_per_hectare} instead.

Units are sanitized as column names. If no logs exist, units come from
the saved product table. Counts include expansion, not raw record counts.

The \code{failed_trees} attribute is a data frame with \code{id}, integer \code{status}
(missing for absent result trees), and \code{expansion} in trees per area.

The same attributes and
columns are returned by \code{\link[=stock_table]{stock_table()}}.
}
\description{
Expand tree counts, basal area, log counts, and product quantities by inventory group.
Retain failures and inventory trees absent from the result.
}
\details{
The function returns weighted sums without variance estimates. Expansion factors are
used as supplied.

A tree contributing several logs to a product is counted once in that product. A tree
contributing to several products occurs in each product group. Do not add product tree
counts or basal areas to obtain stand totals.

Omit \code{product} from \code{by} to count each tree once within its group. Scaled quantities with
different units have separate columns. Quantities sharing a unit are added, so use
comparable product scale and bark rules.
}
\section{Status and missing values}{

\code{merchandising_status} always separates \code{valid}, \code{failed}, and \code{not_in_result}. \code{ok} is
valid volume. \code{no_feasible_log}, \code{no_feasible_log}, is valid zero volume.

Other statuses are failures. Inspect \code{attr(output, 'failed_trees')} and \code{\link[=status_codes]{status_codes()}} to
repair those trees before interpreting the scaled total. When result statuses were omitted,
the saved calculation is rerun with statuses, requiring its models to remain registered.

Trees without logs have a missing \code{product} group label. Valid zero volume contributes zero
logs and quantity. Failed or absent trees contribute missing volume.

Any missing quantity in an applicable group propagates, so incomplete volume is never
reported as zero. When every contributing value for a quantity is missing, one consolidated
warning names each affected quantity and group, including failed and absent volume groups.
Explicit \code{na_action = 'propagate'} stays silent.

Empty sums for valid trees without applicable logs remain zero.
}

\examples{
## Load dplyr
library(dplyr)

## Compile the example inventory with supplied expansion
stand_table(x = merchandise(dbh = example_trees$dbh,
                            ht = example_trees$ht,
                            species = example_trees$species,
                            products = example_products(name = 'pnw'),
                            status = TRUE),
            trees = example_trees \%>\%
            mutate(id = tree),
            expansion = 5,
            by = 'species')
}
\seealso{
\code{\link[=stock_table]{stock_table()}} for product quantities by diameter class,
\code{\link[=product_summary]{product_summary()}} for totals from a selected result table,
\code{\link[=merchandise]{merchandise()}} to calculate the logs first.
}
