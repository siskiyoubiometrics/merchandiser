% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem-smalltapers.R
\name{regional_small_tapers}
\alias{regional_small_tapers}
\title{Inspect regional taper and volume conventions}
\description{
Regional families retain the source National Volume Estimator Library profile paths.
\code{get_taper_model()} returns each identifier's input requirements.
}
\details{
\code{r1_taper} uses the Byrne lodgepole diameter profile and species-specific total
volumes.
\code{r2_taper} includes Max-Burkhart profiles with Czaplewski correction and conditioned profiles
requiring an upper-stem measurement pair.

\code{r5_taper} uses the Wensel and Krumland profile.
\code{r12_taper} retains Sharpneck total volume with supplied \code{form_class}.
Its public diameter calculation uses the source Raile stump fallback.

\code{blm_taper} selects Bureau of Land Management profile groups and source form-class defaults.
\code{behre_taper} uses the source Behre hyperbola. Their total-volume calculations require
\code{form_class} even where diameter calculations can obtain a default.

These families require \code{bark_ratio} for outside-bark calculations.
Default inverse calculations retain the highest crossing of the public profile.
Source compatibility reproduces the source's rounded search and unavailable branches.
}
\section{Unavailable species total}{

\verb{100JB2W019} returns a zero ground-to-tip total because the source leaves its species
index unassigned. The calculation does not diagnose this unavailable species total.
}

\examples{
## Inspect an equation with an unavailable species total
get_taper_model(id = '100JB2W019')

## Check its ground-to-tip volume behavior
stem_volume(dbh = 20,
            ht = 100,
            model = '100JB2W019',
            lower = 0,
            lower_type = 'height',
            status = TRUE)
}
\seealso{
\code{\link[=get_taper_model]{get_taper_model()}}, \code{\link[=dob]{dob()}}, \code{\link[=height_at_dib]{height_at_dib()}}, \code{\link[=stem_volume]{stem_volume()}}.
}
\keyword{internal}
