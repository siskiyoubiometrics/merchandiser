% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.height_fit}
\alias{plot.height_fit}
\title{Inspect measured heights and fitted height curves}
\usage{
## Call signatures
\method{plot}{height_fit}(x, units = 'imperial', ...)
}
\arguments{
\item{x}{One \code{\link[=fit_height]{fit_height()}} result. Required, with retained measured data
(\code{data}) containing diameter \code{dbh} in inches, height \code{ht} in feet,
species code \code{spcd} and grouping label \code{group} (both unitless). Missing
or older fits without measured data are errors.}

\item{units}{One character value, \code{'imperial'} (default) for inches and
feet or \code{'metric'} for centimeters and meters. Omission uses imperial
display even if fitting used metric inputs. Missing or unknown values
error.

Example: \code{units = 'metric'}.}

\item{...}{Named base graphics arguments passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}. Omission uses
labeled axes and species codes as panel titles.

Limits use displayed units, other settings
are unitless. Missing settings follow base graphics behavior.}
}
\value{
The height fit, invisibly. Draws one panel per measured species.
}
\description{
Draw retained height observations and fitted curves from a \code{\link[=fit_height]{fit_height()}} result.
}
\details{
Curves use population predictions without group effects, over
each species' measured diameter range. Points are the valid measured
trees retained by the fit, not completed heights. The dashed pooled
curve is labeled when a species used pooling.
}
\section{Status and missing values}{

Input records rejected during fitting are absent. Pooling is disclosed
in each panel. The plot does not estimate uncertainty or validate the
fit against independent measurements.

Refit older saved fits that do
not retain measured data.
}

\section{Graphical settings}{

\code{main} is one character panel title, with the tree or species label used when omitted.
\code{xlim} and \code{ylim} are numeric pairs in the displayed axis units, with ranges determined from
plotted data when omitted. \code{cex} and \code{cex.main} are numeric size multipliers, unitless, for
points or titles.

\code{pch} selects a base graphics point symbol, for example \code{16} for a solid circle. Missing or
invalid settings follow \code{\link[graphics:plot.default]{graphics::plot.default()}}. Other named base graphics settings are
accepted by that function.
}

\section{Fitted height fields}{

The returned \code{height_fit} is a list. \code{form} names the equation. \code{data} is a data frame of
valid measured rows with numeric \code{dbh} in inches, \code{ht} in feet, integer species code \code{spcd},
and factor \code{group}.

No missing measurement rows remain. \code{fixed_effects} has integer \code{spcd}, character
\code{model_key}, logical \code{pooled}, and numeric \code{a}, \code{b}, \code{c}. These are the coefficients of the
equations in Details, using inches and feet.

\code{c} is missing for forms without a third coefficient.

\code{variance_components} has integer \code{spcd}, character \code{model_key} and
\code{random_parameter}, numeric \code{random_effect_sd} on the fitted parameter
scale, and numeric \code{residual_sd} in feet. \code{random_effects} has character
\code{model_key} and \code{group} plus numeric \code{effect} on that parameter scale.
\code{n_by_species} has integer \code{spcd} and integer \code{n}, the number of retained
measurements. Codes, names, flags, and counts are unitless.

\code{groups_seen} lists character group labels. \code{pooled_species} lists integer species codes
below the count threshold. \code{convergence_pooled_species} lists those whose separate fit
failed.

\code{model_map} maps species to model keys, and \code{models} holds the fitted R models.
\code{internal_fixed_effects} holds transformed coefficients by model key. \code{units} is
\code{'imperial'}, \code{min_n} is the supplied count threshold, and \code{package_version} records the
package version or is missing when unavailable.

None is a data-frame list column. Keep the complete fitted object for prediction.
}

\examples{
## Fit the retained height observations
fit <- fit_height(dbh = example_trees_pnw$dbh,
                  ht = example_trees_pnw$ht_observed,
                  spcd = example_trees_pnw$spcd)

## Draw measured heights and fitted curves
plot(fit)
}
\seealso{
\code{\link[=fit_height]{fit_height()}} to estimate a relationship,
\code{\link[=predict_height]{predict_height()}} for numeric predictions, \code{\link[=complete_heights]{complete_heights()}} to fill
missing heights in an inventory.
}
