% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{product}
\alias{product}
\title{Define a product and its log specifications}
\usage{
## Call signatures
product(product, priority, species = 'all', ...)
}
\arguments{
\item{product}{Required unique, nonempty character product label.}

\item{priority}{Required positive whole number, unique across the table.
Smaller priorities are considered first.}

\item{species}{Species selection, default \code{'all'}. Supply inventory species
codes, names, or the rules described in \link{product_schema}.}

\item{...}{Named specification fields described in \link{product_schema}.
Supply \code{min_sed} and either \code{lengths} or a length range.}
}
\value{
A checked product row. Field types, defaults, and constraints are
described in \link{product_schema}.
}
\description{
Describe accepted log dimensions and their measurement unit. Combine rows
with \code{\link[=products]{products()}} to define the products available within a tree.
}
\details{
\code{volume_unit} is required and selects \code{'scribner'}, \code{'international'},
\code{'doyle'}, \code{'cubic'}, \code{'green_ton'}, or \code{'cord'}. \code{inside_bark} defaults to
\code{TRUE}, \code{split_scale} to \code{FALSE}, and \code{round} to \code{'default'}.
\link[=scaling_rules]{Scaling rules} describes their mapping and additional reporting.

Lengths and trim use feet under imperial calls and meters under metric calls.
Diameter limits use inches or centimeters. The unit system belongs to the
merchandising call. It does not convert supplied measurements.

Acceptance uses physical endpoints. Nominal length excludes trim and defines
the measured log body. Optional \code{price} and \code{price_per} describe a hypothetical
or supplied quote in the product's measurement unit.

Priced runs require a
currency label. An omitted price leaves the product unpriced.
}
\section{Status and missing values}{

Invalid specifications stop the call. A product table carries no calculation
status and does not guarantee that a log fits the supplied tree.
}

\examples{
## Define a sawlog for the shipped species
product(product = 'saw',
        priority = 1,
        species = example_trees$spcd,
        lengths = c(16, 32),
        trim = 0.5,
        min_sed = 6,
        volume_unit = 'scribner',
        inside_bark = TRUE,
        round = 'down')
}
\seealso{
\code{\link[=products]{products()}}, \code{\link[=merchandise]{merchandise()}}, \link{scaling_rules}, \link{product_schema}.
}
