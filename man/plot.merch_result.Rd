% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-felled.R
\name{plot.merch_result}
\alias{plot.merch_result}
\title{Draw a felled stem and its selected logs}
\usage{
## Call signatures
\method{plot}{merch_result}(x, tree = NULL, trees = NULL, col = NULL, legend = TRUE,
  show_value = FALSE, ...)
}
\arguments{
\item{x}{Required \code{merch_result} from \code{\link[=merchandise]{merchandise()}} or \code{\link[=optimize_bucking]{optimize_bucking()}},
retaining its recorded call. Missing is an error. Its tables and measurement
units are described in \link[=result_tables]{Result tables}.}

\item{tree}{One nonmissing atomic identifier matching \code{x$trees$id}, unitless.
Default \code{NULL} selects the first tree when \code{trees} is also omitted.
Unknown identifiers are errors.}

\item{trees}{Atomic vector of distinct, nonmissing identifiers matching
\code{x$trees$id}, unitless. Default \code{NULL} uses \code{tree} or the first tree.
Supply only \code{tree} or \code{trees}. Empty or unknown selections are errors.}

\item{col}{Vector of base graphics colors, one per product in \code{x$logs},
optionally named by product. Default \code{NULL} selects colors by product name. Missing colors are
errors.

Unitless. Example: \code{c(saw = 'steelblue')}.}

\item{legend}{One nonmissing logical value, default \code{TRUE} to draw the shared
product and line key. Example: \code{legend = FALSE}. Unitless.}

\item{show_value}{One nonmissing logical value, default \code{FALSE}. Use \code{TRUE}
to add priced log totals and their currency to labels or the key.}

\item{...}{Named base graphics settings passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}. Omission uses
labeled axes and the result's units.

Example: \code{main = 'volume'}. Missing settings follow base graphics behavior. Axis limits use
plotted
coordinates.

Other settings are unitless.}
}
\value{
The original result, invisibly, with tables described in \link[=result_tables]{Result tables}.
}
\description{
Draw selected logs, residual sections, and recorded defects from a merchandising result.
}
\details{
Solid outlines show inside bark and dashed outlines show outside bark. Colored log
bodies stop at nominal ends. Pale hatched trim ends at physical cuts.

Gray residuals carry cause labels. Defects retain their recorded locations and
categories. Unlocated percentage deductions are labeled without invented locations.

Missing profiles receive explanatory rows. Custom models must remain registered. Labels use
measured text extents.

Crowded labels switch to numbered keys below the stem. Each key lists product, length,
inside-bark small-end diameter, and quantity. \code{show_value} adds priced totals.

Row heights include all lanes and key rows. Text and stems shrink together when the active panel
cannot hold their preferred
size. The drawing occupies one panel and respects an existing \code{par(mfrow)} setting.
}
\section{Status and missing values}{

The drawing adds no status codes. Missing profiles receive a labeled row. Review tree status
before interpreting partial logs or missing quantities.

\code{no_feasible_log} means a valid result with no logs. Other nonzero codes require review
using \code{\link[=status_codes]{status_codes()}}. Invalid identifiers or colors stop the call.
}

\examples{
## Calculate logs for the shipped tree list
result <- merchandise(dbh = example_trees$dbh,
                      ht = example_trees$ht,
                      model = example_trees$model,
                      species = example_trees$species,
                      products = example_products(name = 'pnw'),
                      status = TRUE)

## Inspect the result
plot(result, tree = 5)
}
\seealso{
\link[=result_tables]{Result tables} for plotted quantities, \code{\link[=assumptions]{assumptions()}} for saved
choices, and \code{\link[=plot.stem_profile]{plot.stem_profile()}} for an uncut profile.
}
