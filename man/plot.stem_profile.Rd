% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.stem_profile}
\alias{plot.stem_profile}
\title{Draw a stem profile}
\usage{
## Call signatures
\method{plot}{stem_profile}(x, tree = NULL, trees = NULL, ...)
}
\arguments{
\item{x}{One data frame returned by \code{\link[=stem_profile]{stem_profile()}}, retaining its class
and units attribute. Required. Missing is an error.

Columns \code{id}, \code{h}, \code{dib}, \code{dob}, and \code{status} supply the drawing. Height uses feet or meters,
and diameters use inches or centimeters. Identifiers and statuses are unitless.

Cumulative volumes are unused.}

\item{tree}{One nonmissing identifier matching \code{x$id}, for example
\code{example_trees$tree[1]}. Unitless. Default \code{NULL} selects the first tree
when \code{trees} is also omitted.

Unknown identifiers are errors.}

\item{trees}{Atomic vector of distinct, nonmissing identifiers matching \code{x$id}. Default \code{NULL}
uses \code{tree} or the first tree. Supply only one selector.

Empty or unknown selections are errors. Unitless.}

\item{...}{Named base graphics arguments passed to \code{\link[graphics:plot.default]{graphics::plot.default()}}. Default axes use
the saved units and the title is the tree identifier.

Example: \code{main = 'Stem dimensions'}. Axis limits use plotted units. Other settings are unitless.

Missing settings follow base graphics.}
}
\value{
The profile data frame, invisibly. Draws one panel per selected tree.
}
\description{
Draw inside- and outside-bark profiles from a \code{\link[=stem_profile]{stem_profile()}} result.
}
\section{Status and missing values}{

Missing outside-bark diameters omit that line. A tree without enough
finite inside-bark measurements receives a labeled empty panel. Inspect
\code{x$status} with \code{\link[=status_codes]{status_codes()}} and repair the original model inputs.
}

\section{Graphical settings}{

\code{main} is one character panel title, with the tree or species label used when omitted.
\code{xlim} and \code{ylim} are numeric pairs in the displayed axis units, with ranges determined from
plotted data when omitted. \code{cex} and \code{cex.main} are numeric size multipliers, unitless, for
points or titles.

\code{pch} selects a base graphics point symbol, for example \code{16} for a solid circle. Missing or
invalid settings follow \code{\link[graphics:plot.default]{graphics::plot.default()}}. Other named base graphics settings are
accepted by that function.
}

\section{Profile table fields}{

\describe{
\item{\code{id}}{Input atomic tree label, unitless, for example \code{'example-1'}.}
\item{\code{h}}{Numeric height above ground in feet or meters. Failed trees
can have a missing height. }
\item{\code{dib}, \code{dob}}{Numeric inside-bark and outside-bark diameters,
respectively, in inches or centimeters. Unavailable values are missing.}
\item{\code{cum_volume_ib}, \code{cum_volume_ob}}{Numeric cumulative inside-bark
and outside-bark volume from the lower limit, in cubic feet or cubic
meters. The first available value is zero. Missing values are unavailable.}
\item{\code{status}}{Integer unitless calculation code. Zero means completion.
A failed tree retains a diagnostic row. Check quantity availability as
well as this code because outside-bark failure can coexist with a diameter.}
}
}

\examples{
## Calculate profiles for the example trees
profile <- stem_profile(dbh = example_trees$dbh,
                        ht = example_trees$ht,
                        model = example_trees$model,
                        id = example_trees$tree)

## Draw the selected tree
plot(profile, tree = 5)
}
\seealso{
\code{\link[=stem_profile]{stem_profile()}} to calculate the displayed measurements,
\code{\link[=plot.merch_result]{plot.merch_result()}} to add product cuts and located conditions.
}
