% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{product_summary}
\alias{product_summary}
\title{Sum volume by product or tree group}
\usage{
## Call signatures
product_summary(x, table = 'trees', group = NULL, expansion = 1, basis = 'net', na_action =
  'exclude')
}
\arguments{
\item{x}{Required complete \code{merch_result}, such as \code{result} below. Missing or
other objects are errors. Its tables and fields are described in \link[=result_tables]{Result tables}
.}

\item{table}{One character name, \code{'trees'} (default), \code{'logs'}, \code{'scales'}, or
\code{'values'}. Missing or unknown names are errors. Unitless.

Example: \code{'logs'}.}

\item{group}{Default \code{NULL} gives no additional grouping. Otherwise supply a
character vector of tree-column names or a uniquely named list of atomic
vectors, each length one or one per result tree. Group labels are
unitless.

Example: \code{list(stand = 'A')}. Missing grouping labels are
excluded by the grouping calculation, so resolve them before reporting.}

\item{expansion}{Finite nonnegative numeric weights, length one or exactly one
per result tree in tree-table order. Default \code{1} sums the supplied records. Missing values are
errors.

Example: \code{5} represents five trees per record
in the analyst's stated population or area basis. No plot design is inferred.}

\item{basis}{One character value, \code{'net'} (default) or \code{'gross'}. It chooses log,
scale, or value quantities. For \code{table = 'trees'}, all recognized cubic,
value, and log-count fields are summed regardless of this choice.

Missing choices are errors. Unitless. Example: \code{basis = 'gross'}.}

\item{na_action}{One character value, default \code{'exclude'}, excludes
missing quantities when at least one quantity is observed. An entirely
missing quantity returns \code{NA}, including under the default. \code{'propagate'} returns \code{NA} if any
quantity is missing.

Missing or
unknown choices are errors. Unitless. Example: \code{na_action = 'propagate'}.}
}
\value{
A grouped data frame of weighted quantity totals and unweighted
tree counts. \code{input_trees} counts distinct contributing tree identifiers,
\code{valid_trees} counts completed and valid zero-log trees, \code{failed_trees} counts other
statuses, and \code{valid_zero_log_trees} counts \code{no_feasible_log}.

These count
columns are record counts, not expanded trees per area.

Use
\code{\link[=stand_table]{stand_table()}} to retain the complete inventory and expanded counts.
}
\description{
Sum quantities from a saved result by product or caller-supplied groups. Measurement units
and procedures remain explicit in the grouping fields.
}
\section{Status and missing values}{

An entirely missing requested quantity returns \code{NA}. One consolidated
warning names each affected quantity and group. Explicit
\code{na_action = 'propagate'} suppresses this warning and also propagates
partially missing quantities. Counts describe tree calculation status,
not the availability of individual quantities.
}

\section{Grouping and returned totals}{

Log groups always retain \code{product}, \code{scale_rule}, \code{measurement_quantity}, \code{scale_unit}, and
\code{scale_bark_basis}. Scale groups retain \code{scale_rule}, \code{unit}, and \code{bark_basis}. Named
reporting also retains \code{name} and \code{is_product}.

Value groups retain \code{product} and \code{currency}. Requested group columns precede these labels.
Labels keep the meanings and units in \link[=result_tables]{Result tables}.

No rounding is applied to totals.

For logs, net totals are \code{log_net_cubic_ib}, \code{log_net_cubic_ob}, and \code{net_scale}. Gross
totals use the corresponding \code{log_gross_cubic_ib}, \code{log_gross_cubic_ob}, and \code{gross_scale}.
Scales return \code{net} or \code{gross}, and values return \code{net_value} or \code{gross_value}.

Tree summaries include every cubic, value, and \code{log_count} column present. Multiply the
source unit by the supplied expansion basis to interpret each total.

The integer, unitless counts \code{input_trees}, \code{valid_trees}, \code{failed_trees},
and \code{valid_zero_log_trees} count contributing records, not expanded trees. An empty source table
returns a data frame with no columns. If statuses
were omitted from the result, contributing trees are assumed completed.

Create the result with \code{status = TRUE} when failure counts matter.
}

\examples{
## Calculate logs for the shipped tree list
result <- merchandise(dbh = example_trees$dbh,
                      ht = example_trees$ht,
                      model = example_trees$model,
                      species = example_trees$species,
                      products = example_products(name = 'pnw'),
                      status = TRUE)

## Inspect the result
product_summary(x = result, table = 'logs')
}
\seealso{
\code{\link[=stand_table]{stand_table()}} for complete tree accounting, \code{\link[=product_summary_by_tree]{product_summary_by_tree()}}
to calculate and sum logs in one call, \code{\link[=assumptions]{assumptions()}} for saved choices.
}
