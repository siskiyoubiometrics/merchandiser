% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem-nsvb.R
\name{nvel_volume}
\alias{nvel_volume}
\title{Compare the source library's volume components}
\usage{
## Call signatures
nvel_volume(dbh, ht, model, rules = nvel_rules(), id = NULL, ..., units = 'imperial', status =
  FALSE)
}
\arguments{
\item{dbh}{Required positive finite numeric diameter at breast height
outside bark, in inches for imperial units or centimeters for metric
units. Length one repeats, otherwise supply one value per tree. Omission
is an error.

Missing or nonfinite values give \code{na_input} and a missing result. Example: \code{example_trees$dbh}.}

\item{ht}{Required positive finite numeric total height above ground, in
feet for imperial units or meters for metric units. Length one repeats,
otherwise supply one value per tree. Omission is an error.

Missing or
nonfinite values give \code{na_input}. Example: \code{example_trees$ht}.}

\item{model}{Required character vector or factor of equation identifiers,
length one or one per tree. Unitless. Omission is an error, missing values
give \code{na_input}, and unknown identifiers give \code{unknown_model}.

Example:
\code{example_trees$model}. No replacement equation is chosen here.}

\item{rules}{A list made by \code{\link[=nvel_rules]{nvel_rules()}}, default \code{nvel_rules()}. Its fields are listed in the
corresponding section. Omission requests source defaults where
the chosen equation and region provide them.

A missing object or other
class is an error.}

\item{id}{Optional character identifiers, length one or one per tree. Default \code{NULL} omits the
column. Missing labels are retained, and labels
need not be unique.

Unitless. Example: \code{example_trees$tree}.}

\item{...}{Named equation inputs listed in the corresponding section, with optional numeric
\code{spcd}, \code{division}, and \code{region} described in the source selection section. Length one or one
per tree.

Unknown names stop the call.}

\item{units}{One character value, \code{'imperial'} (default) or \code{'metric'}. Omission selects inches,
feet, and cubic feet. Metric selects centimeters,
meters, and cubic meters.

Missing and unknown values are errors. Example: \code{units = 'metric'}.}

\item{status}{One nonmissing logical flag, default \code{FALSE} for row warnings.
\code{TRUE} adds an integer status field for each component and both log counts.
Unitless. Example: \code{status = TRUE}.}
}
\value{
A data frame with the following numeric source components in source
order, optional character \code{id}, integer \code{n_logs_primary} and
\code{n_logs_secondary}, and integer \code{errflag}. Counts are unitless source
log counts, not counts of merchandising-result rows. The error flag is
the source error number when a source error was returned, or the package
status otherwise.

Component status columns append \verb{_status} to each
quantity and count name. There are no list columns or user-filled defaults.
}
\description{
Return source-library volume components for fixture comparison. Unsupported components
remain missing.
}
\details{
Source defaults are applied before equation evaluation. Ordinary
stem equations supply total and stump volume, with unsupported log scales
missing. The national biomass equations supply their source volume record,
while merchantable components unavailable through this comparison remain
missing. This function does not select the logs returned by \code{\link[=merchandise]{merchandise()}}.
}
\section{Source selection through dots}{

\code{spcd} is a numeric inventory species code, unitless, defaulting to the identifier's final
three characters. \code{division} is a numeric ecological code, unitless, derived from positions
five through seven and the mountain prefix when omitted. \code{region} is a numeric source
region, unitless, derived from the first identifier character when omitted, with \code{A}
interpreted as 10 and other nondigits as zero.

These values are not independently checked against complete geographic tables here. Supply
recognized finite whole-number codes for source comparisons. Examples are \code{spcd = 202},
\code{division = 1240}, and \code{region = 6}.

Missing values do not request a second geographic lookup. Use the exported source lookup
functions for assignments.
}

\section{Volume component fields}{

\code{vol_total_cu} is total inside-bark volume. \code{vol_bf_gross} and \code{vol_bf_net}
are gross and net primary-product board-foot quantities. \code{vol_cu_gross}
and \code{vol_cu_net} are gross and net primary-product cubic volume.

\code{cords}
is its source cord quantity. \code{vol_top_cu_gross} and \code{vol_top_cu_net} are
secondary-product cubic quantities, and \code{cords_top} is the corresponding
cord quantity. These merchantable fields are currently unavailable in
this comparison and are returned missing with \code{capability_missing}.

\code{vol10}, \code{vol11}, \code{vol12}, and \code{vol13} retain source-array positions whose measurement
definitions are not supplied by this source comparison. Do not interpret them as
merchantable quantities. Their source units are not converted by a metric call.

Ordinary stem equations set \code{vol11} and \code{vol13} to zero for source comparison and leave
\code{vol10} and \code{vol12} missing. \code{vol_stump_cu} is source stump volume and \code{vol_tip_cu} is
source tip volume. Imperial cubic fields use cubic feet.

Board feet and cords retain source units. The metric conversion limitation applies to ordinary
stem equations.
}

\section{Metric limitation}{

For ordinary stem equations, total and stump
volumes already use the requested units before the final metric conversion is applied again.
Thus those metric values are converted twice and must not be used as correct cubic meters.

The national biomass equation record starts in cubic feet and receives the intended
conversion. Use \code{\link[=stem_volume]{stem_volume()}} for bounded cubic volume.
}

\section{Status and missing values}{

Zero means an available source
comparison quantity, not necessarily merchantable volume.

Missing inputs, invalid dimensions, unknown equations, unavailable components, and source
failures return missing quantities. \code{\link[=status_codes]{status_codes()}} describes the input and model diagnoses.
Source failures retain their library error offset.

\code{species_out_of_scope} retains an out-of-scope value. \code{not_unique} retains the highest crossing.

Partial component availability does not justify filling other components with zero.
}

\section{Equation inputs through dots}{

Inputs are used only where declared by the selected equation. Numeric
inputs accept integers and doubles. Each input has length one or the common
number of trees, with units following the call. For a particular model,
\code{get_taper_model(model)$inputs} lists required, optional, and paired inputs.

\describe{
\item{\code{bark_ratio}}{Numeric ratio of inside-bark to outside-bark diameter,
strictly greater than zero and at most one. Unitless.
Omission uses a model ratio where supplied.
An explicit missing ratio gives \code{na_input}, not the default.}
\item{\code{upper_ht1}, \code{upper_ht2}}{Positive finite numeric upper measurement
heights above ground, in feet or meters.
Omission supplies no measurement. A required missing height gives \code{missing_input}
when omitted or \code{na_input} when supplied as missing.}
\item{\code{upper_d1}, \code{upper_d2}}{Positive finite numeric diameters at the
corresponding upper heights, in inches or centimeters.  Supply each diameter with its paired
height where the model requires
a pair. An incomplete pair gives \code{missing_input}.}
\item{\code{upper_bark}}{Character \code{'ib'} or \code{'ob'} for the upper
measurements, unitless. Example: \code{'ib'}. Omission uses the model
convention, which is inside bark for Flewelling upper measurements.
A missing supplied label gives \code{na_input}.}
\item{\code{form_class}}{Positive numeric equation form class, unitless.
Its definition follows the selected equation. Omission
uses its default only when optional. Missing supplied values give \code{na_input}.}
\item{\code{site_index}}{Positive numeric site-index height, in feet or meters.
The equation determines species and reference age.
Omission uses its defaults only when optional. Missing gives \code{na_input}.}
\item{\code{basal_area}}{Positive numeric stand basal area, in square feet per
acre or square meters per hectare.  Omission uses the
model's defaults only when optional. Missing gives \code{na_input}.}
\item{\code{decay_class}}{Whole-number class from 1 through 5, unitless.
This is an equation input only where declared, with no
universal default. Missing gives \code{na_input}. The separate biomass function
also accepts zero for a live tree.}
\item{\code{cull}}{Numeric percentage from zero through 100.
Omission supplies no override. Missing gives \code{na_input} where declared.
This does not create located defect records.}
\item{\code{spcd}}{Positive whole-number Forest Inventory and Analysis tree
inventory species code within R's integer range, unitless.
Omission skips the species-scope check. Supplied codes can return \code{na_input}, \code{unknown_species},
or \code{species_out_of_scope}.}
}
}

\section{Source rule fields}{

Each field has exactly one value in this translation.
\describe{
\item{\code{even_or_odd}}{National Volume Estimator Library \code{EVOD} segment-length code
(unitless): \code{1} permits
odd nominal lengths and \code{2} permits only even nominal lengths. Options 11
through 14 define their own fixed-length behavior. \code{NA} requests the
library default.

Numeric nonempty vector. Accepted finite values are 1 or 2. Default \code{NA} applies when omitted.

Numeric \code{NA} leaves a source
default unset. Infinite values are errors. }
\item{\code{option}}{source library \code{OPT} segmentation code (unitless). Codes 11, 12, 13,
and 14 use the library's 16-, 20-, 32-, and 40-foot scale conventions. Codes 21 through 24
select the four nominal-log top-segment rules.

\code{NA}
requests the library default. Numeric nonempty vector. Accepted finite values are 11 through
14 or 21 through 24.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{maximum_length}}{source library \code{MAXLEN}, the maximum nominal segment length, in
the consuming call's height units. Numeric nonempty vector. Accepted finite values are greater
than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{minimum_length}}{source library \code{MINLEN}, the minimum nominal segment length, in
the consuming call's height units. Numeric nonempty vector. Accepted finite values are greater
than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{minimum_top_length}}{source library \code{MINLENT}, the minimum accepted top-log
length, in the consuming call's height units. Numeric nonempty vector. Accepted finite values
are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{merchantable_length}}{source library \code{MERCHL}, the minimum length of primary
product required for the tree to be merchantable, in the consuming call's
height units. Numeric nonempty vector. Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{primary_top}}{source library \code{MTOPP}, the primary-product top diameter inside
bark used for board-foot, cubic, and cord volume, in the consuming call's
diameter units. Numeric nonempty vector. Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{secondary_top}}{source library \code{MTOPS}, the secondary-product top diameter inside
bark used for cubic and cord volume, in the consuming call's diameter
units. Numeric nonempty vector. Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{stump}}{source library \code{STUMP}, the groundline-to-stump height, in the consuming
call's height units. Numeric nonempty vector. Accepted finite values are at least zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{trim}}{source library \code{TRIM}, the trim allowance added to each segment, in the
consuming call's height units. Numeric nonempty vector. Accepted finite values are greater
than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{bark_ratio}}{The source library bark-ratio auxiliary represented by \code{merchandiser}
as the unitless constant diameter-inside-bark to diameter-outside-bark
ratio in \verb{(0, 1]}. \code{NA} leaves bark to the selected model or library
default. Numeric nonempty vector.

Accepted finite values are greater than zero and at most one. Default \code{NA} applies when omitted.
Numeric \code{NA} leaves a source
default unset.

Infinite values are errors. }
\item{\code{minimum_board_foot_dbh}}{source library \code{MINBFD}, the minimum outside-bark diameter
at breast height
for board-foot eligibility, in the consuming call's diameter units. Numeric nonempty vector.
Accepted finite values are greater than zero.

Default \code{NA} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{scribner}}{source library \code{COR} Scribner-volume mode (unitless): \code{'table'} uses
the Scribner table, \code{'factor'} uses factors, and \code{'regional'} requests
the regional library default.
Nonempty character vector, default \code{'regional'} when omitted.
Missing elements and unknown labels are errors. Example: \code{scribner = 'regional'}.}
\item{\code{prod}}{source library two-character product code (unitless), such as \code{'01'} for
sawtimber or \code{'08'} for a nonsaw product.
Nonempty character vector, default \code{'01'} when omitted.
Missing elements and unknown labels are errors. }
\item{\code{ht_type}}{source library \code{HTTYPE} code (unitless): \code{'F'} denotes a physical
height, \code{'L'} denotes a height expressed in logs, and \code{''} leaves the
field unspecified for the consuming call.
Nonempty character vector, default \code{''} when omitted.
Missing elements and unknown labels are errors. Example: \code{ht_type = ''}.}
\item{\code{live}}{source library live/dead status code (unitless): \code{'L'} for live or \code{'D'}
for dead.
Nonempty character vector, default \code{'L'} when omitted.
Missing elements and unknown labels are errors. Example: \code{live = 'L'}.}
\item{\code{ctype}}{Source call-origin code, unitless. \code{'C'} selects cruise, \code{'I'} selects Forest
Inventory and Analysis, and \code{'F'} selects Forest Vegetation Simulator. \code{'B'} selects the
alternate National Scale Volume and Biomass route.

Nonempty character vector, default \code{'C'} when omitted. Missing elements and unknown labels are
errors. Example: \code{ctype = 'C'}.}
\item{\code{cull}}{source library \code{CULL}, the whole-tree cull deduction as a percentage from
0 through 100. Numeric nonempty vector. Accepted finite values are zero through 100.

Default \code{0} applies when omitted. Numeric \code{NA} leaves a source
default unset. Infinite values are errors.

}
\item{\code{forest}}{source library two-digit national-forest code represented as an integer
from 0 through 99 (unitless). Zero means no forest-specific selection. Numeric nonempty
vector.

Accepted finite values are whole numbers from zero through 99. Default \code{0} applies when omitted.
Numeric \code{NA} leaves a source
default unset.

Infinite values are errors. }
\item{\code{district}}{source library two-digit ranger-district code represented as an
integer from 0 through 99 (unitless). Zero means no district-specific
selection. Numeric nonempty vector.

Accepted finite values are whole numbers from zero through 99. Default \code{0} applies when omitted.
Numeric \code{NA} leaves a source
default unset.

Infinite values are errors. }
}
}

\examples{
## Calculate bounded volume for the shipped trees
stem_volume(dbh = example_trees$dbh,
            ht = example_trees$ht,
            model = example_trees$model)
}
\seealso{
\code{\link[=stem_volume]{stem_volume()}} for cubic volume, \code{\link[=biomass]{biomass()}} for biomass,
\code{\link[=merchandise]{merchandise()}} for proposed logs and scaled quantities.
}
\keyword{internal}
