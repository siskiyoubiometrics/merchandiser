% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merchandise.R
\name{optimize_bucking}
\alias{optimize_bucking}
\title{Select log cuts for the greatest value or volume}
\usage{
## Call signatures
optimize_bucking(dbh, ht, model = NULL, products = NULL, id = NULL, spcd = NULL, age = NULL,
  pruned = NULL, defects = NULL, stump_ht = NULL, utilization_top = NULL,
  utilization_top_basis = 'ib', utilization_height = NULL, curvature_scale = NULL, report_also
  = NULL, currency = NULL, objective = 'auto', unpriced = 'zero', ..., units = 'imperial',
  status = FALSE, species = NULL, taper_map = NULL, preset = NULL, quiet = FALSE, region =
  NULL, forest = NULL, district = NULL, scaling = NULL)
}
\arguments{
\item{dbh}{Numeric tree diameters at breast height outside bark. Required. Use inches with
imperial units or centimeters with metric units.

Must be positive and finite for a valid tree. Missing or nonfinite measurements produce a failed
tree record.}

\item{ht}{Numeric total tree heights above ground. Required. Use feet
with imperial units or meters with metric units.

These are total heights, not merchantable heights. Must be positive
and finite for a valid tree. Missing heights are not fitted here.

Complete them first with \code{\link[=complete_heights]{complete_heights()}}.}

\item{model}{Character or factor vector of registered taper equation
identifiers, for example \code{'F00FW2W202'}. Default \code{NULL} uses
\code{taper_map}, or the geographic defaults if no map is supplied. An explicit model takes
precedence over \code{taper_map}.

Unknown or missing
identifiers do not request a replacement equation. Unitless.}

\item{products}{A product data frame, usually made with \code{\link[=product]{product()}}
and \code{\link[=products]{products()}}. The fields, units, defaults, and accepted
measurement rules are described in \link[=product_schema]{Product specification schema}. Default
\code{NULL} loads the
selected illustrative preset.

An explicitly supplied table takes
precedence. Invalid or incomplete specifications stop the call. Example: \code{products = example_products(name = 'pnw')}.}

\item{id}{Atomic vector of unique, nonmissing tree identifiers, such as
\code{c('A-1', 'A-2')}. Default \code{NULL} assigns sequential row numbers after
input lengths are resolved. Required when a nonempty defect table is
supplied.

Use the same identifier type in that table. Unitless.}

\item{spcd}{Species identifiers, supplied as positive whole-number
inventory species codes, recognized symbols, common names, or
scientific names. Default \code{NULL}. Supply species
for automatic equation selection, species-restricted products, or
green-weight calculations.

Missing required species can leave a tree
unresolved or a scaled quantity unavailable. Unitless.}

\item{age}{Numeric tree ages, default \code{NULL}. Use years and the same total-age or
breast-height-age convention as
the product table. Required only when a relevant product limits age.

Missing required age is a missing product attribute, not age zero.}

\item{pruned}{Logical or numeric \code{0}/\code{1} pruning indicators, default
\code{NULL}. Example: \code{pruned = TRUE}. Required only when a relevant
product requires a pruning state.

Missing required values are missing
product attributes. Unitless. No pruning height is inferred.}

\item{defects}{A defect data frame made with \code{\link[=defect]{defect()}} or
\code{\link[=defects_from_stoppers]{defects_from_stoppers()}}. Default \code{NULL} means no recorded defects
are applied. An empty table has the same effect.

Required fields are
\code{id}, \code{from}, \code{to}, and \code{effect}, with conditional fields described
in \code{\link[=defect]{defect()}}. Heights use the call's height units. Missing fields or invalid
record structure may stop the call.}

\item{stump_ht}{Numeric stump heights above ground, default \code{NULL}. Omission uses 1 foot for
imperial calls or 0.3 meter for metric calls. These are the package's two defaults, not exact
conversions of each
other.

Supplied values must satisfy
\verb{0 <= stump_ht < ht}. An explicit \code{NA} does not request the default. Supply a finite height
for each tree being calculated.}

\item{utilization_top}{Numeric minimum utilization diameters, default
\code{NULL}. Units are inches or
centimeters. When omitted, each tree uses the minimum small-end
diameter and bark basis of its last reachable product before defects
are applied.

A supplied zero requests the tree tip. A missing value
or a diameter without a usable crossing gives \code{utilization_top_invalid}.}

\item{utilization_top_basis}{One character value, \code{'ib'} for inside
bark or \code{'ob'} for outside bark. Default \code{'ib'}. Used for an
explicitly supplied \code{utilization_top}.

The omitted-top calculation
instead uses the selected product's bark basis. Missing is invalid. Example:
\code{utilization_top_basis = 'ib'}.

Unitless.}

\item{utilization_height}{Numeric upper height limits above ground,
default \code{NULL} for no additional height cap. Values use feet or
meters and must be nonnegative and finite, or \code{NA} for no cap on that
tree.  The effective top is the
lowest of this cap, the diameter limit, total height, and the earliest
recorded break.}

\item{curvature_scale}{A list with names \code{order} and \code{ratio_upper}, in
that order. Default \code{NULL}. Required when a defect supplies a sweep
or crook category or a product limits one.

\code{order} is a unique,
nonmissing character vector from least to greatest severity. \code{ratio_upper} is a numeric vector
of equal length, strictly increasing
from zero to a final \code{Inf}, with finite intervening values. Both components are unitless.

Current
eligibility uses category order. The bounds are validated metadata,
not an equation that converts measured sweep into a scale deduction. Invalid lists or unknown
product maximum categories stop the call.

A defect category absent from the scale gives tree \code{unknown_curvature_category}.}

\item{report_also}{Character procedure names or a data frame of named
measurements, default \code{NULL} for none. Missing definitions are invalid. Units follow each
definition.

See \link[=scaling_rules]{Scaling rules} for fields, rounding, and examples. These
measurements
use the selected cuts without changing prices or selecting logs again.}

\item{currency}{One nonempty character label for the currency of all
supplied prices, default \code{NULL}. Required if any product has a price. Example: \code{currency = 'USD'} for United States dollars.

Missing is
invalid when prices are present. This labels values without converting
currencies or establishing whether prices are delivered or stumpage.}

\item{objective}{One character value, \code{'auto'} (default), \code{'value'}, or
\code{'net_cubic_ib'}. Automatic selection uses value if any product is priced,
otherwise net inside-bark physical cubic volume. Unitless.

Missing or
unknown choices are errors. Example: \code{objective = 'value'}.}

\item{unpriced}{One character value, \code{'zero'} (default), \code{'exclude'}, or
\code{'error'}. With a value objective these respectively assign zero value,
remove unpriced products, or reject an incomplete price table. Removing
every product is an error.

Unitless. Missing choices are errors. Example: \code{unpriced = 'error'}.}

\item{...}{Uniquely named auxiliary vectors needed by the chosen taper
equations, listed in the corresponding section. Omitted auxiliaries use model defaults
where provided.

A missing required auxiliary produces a failed tree. Unknown names, invalid types, and invalid
supplied domains are errors. These are equation inputs, not additional
product fields or green-weight moisture overrides.}

\item{units}{One character value, \code{'imperial'} or \code{'metric'}. Default \code{'imperial'}. Imperial
inputs use inches for diameters and
feet for lengths and heights, with physical volume in cubic feet.

Metric inputs use centimeters and meters, with physical volume in
cubic meters. Measurement units are specified separately in \code{products}. Missing or other
spellings are invalid.

Example: \code{units = 'metric'}.}

\item{status}{One logical or \code{0}/\code{1} value, default \code{FALSE}. \code{TRUE} adds status columns to tree
and log tables and a \code{diagnostics}
table, while suppressing tree-status warnings. \code{FALSE} reports
nonzero tree statuses in warnings.

Structural errors still stop either
call. Missing is invalid. Example: \code{status = TRUE}.

Unitless.}

\item{species}{An alternative to \code{spcd}, with the same accepted inputs
and default \code{NULL}. Example: \code{species = 'Douglas-fir'}. If both
arguments are supplied, they must resolve to the same species.

A recognized species is also accepted in the third positional
argument, but using the named argument avoids confusion with \code{model}. Missing behavior is the
same as for \code{spcd}. Unitless.}

\item{taper_map}{A data frame containing \code{model} and either \code{spcd} or
\code{species}, with one equation assignment per species. Default \code{NULL}.

Species spellings are resolved and equation identifiers and species
scopes are checked. Missing or duplicate assignments are invalid. A tree species absent from a
supplied map gets \code{model_unresolved} rather than
an automatic replacement equation.

Explicit \code{model} takes precedence. All fields are unitless.}

\item{preset}{One name returned by \code{\link[=example_product_names]{example_product_names()}}, default \code{NULL}. Example: \code{preset = 'pnw'}. Selects an illustrative product
table if \code{products} is omitted, and provides geographic defaults if
needed.

Other names include \code{'us_south'} and \code{'douglas_fir'}. Missing or unknown
names are invalid. These are examples to review, not verified product
specifications.

Unitless.}

\item{quiet}{One logical or \code{0}/\code{1} value, default \code{FALSE}. \code{TRUE} suppresses the informational
defaults message. It does not
suppress tree-status warnings or errors.

Missing is invalid. Example: \code{quiet = TRUE}. Unitless.}

\item{region}{One numeric Forest Service region code or a preset name,
default \code{NULL}. Used for source-library
equation defaults when explicit equations are absent. Valid library
region codes are 1 through 10.

Omission uses the selected preset,
otherwise the southern example for an entirely loblolly pine species
vector and the Pacific Northwest example for other inputs. This is not an
inference of geographic location. Missing is invalid.

Unitless.}

\item{forest}{One numeric whole-number Forest Service forest code from
zero through 99, default \code{NULL}. Omission uses the selected geographic settings. The Pacific
Northwest example
uses 12 and the southern example uses 8.

This is an agency code, not
the name or identifier of the user's stand. Missing is invalid. Unitless.}

\item{district}{One numeric whole-number Forest Service district code
from zero through 99, default \code{NULL}. Omission uses the selected settings, currently zero in
both example
presets. Other numeric regions begin with forest and district zero
unless overridden.

Missing is invalid. Unitless.}

\item{scaling}{Deprecated alternative to \code{report_also}, with the same default
\code{NULL}. Supply only one. Use \code{report_also = 'huber_ft3_ib'} in new code.}
}
\value{
A \code{merch_result} with cuts, tree totals, scales, residuals, deductions,
values, and recorded settings described in \link[=result_tables]{Result tables}. The saved
\code{algorithm} label is \code{'dp'}, meaning cuts were compared for the selected
objective. Physical cubic units follow \code{units}, and measurement units and
currency follow the product table and call.
}
\description{
Select cuts that maximize value or net inside-bark cubic volume under the supplied product
specifications. Return the same result structure as \code{\link[=merchandise]{merchandise()}}.
}
\section{Product priority and cut selection}{

The current product must be used while it has a feasible log and has not reached its per-
section count limit. A higher-paying lower-priority product cannot displace a feasible
current product. Progression to lower products requires \code{allow_lower_products = TRUE}.

A cull section restarts product selection and counts. Sound wood cannot be skipped to reach
a better log. These constraints define the alternatives being compared.

Physical cubic volume includes trim. Scaled quantity follows the product's nominal body and
scaling procedure. Located rot reduces those quantities before value is compared.

Later percentage deductions do not trigger new cuts. Source-library length policies retain
their specified sequence.
}

\section{Numerical methods}{

The optimization compares feasible continuations from each cutting height,
product, and count.
The procedure does not fit taper equations or estimate uncertainty in
prices or tree dimensions.
}

\section{Input length and equation selection}{

Tree measurements, identifiers, species, tree attributes, height limits, and equation
auxiliaries must have one common length. A length-one value is repeated for all trees.
Identifiers must still be unique after this step.

Product, defect, mapping, and extra-scale tables have their own row counts. They are not
repeated to the inventory length.

Explicit \code{model} and \code{products} bypass geographic defaults. \code{assumptions()} records
automatically selected settings.

The species-to-equation map is checked against registered species scopes. Supplying a model
directly is not a substitute for that review.

Products requiring outside-bark dimensions can trigger a species-reference bark ratio lookup
when the model lacks an outside-bark equation. The result records this assumption. Otherwise
supply \code{bark_ratio}
explicitly or choose a model that provides the needed dimensions.
}

\section{Equation auxiliaries}{

Each supplied auxiliary is numeric unless stated otherwise, with one
value or one per tree. It is used only by models declaring that input.
Model-specific defaults and requirements can be inspected with
\code{\link[=get_taper_model]{get_taper_model()}} and \code{\link[=check_taper_models]{check_taper_models()}}.
\describe{
\item{\code{bark_ratio}}{Inside-bark diameter divided by outside-bark
diameter, greater than zero and at most one. Omission uses the model's ratio or the
automatic species lookup
listed in the corresponding section. If none is available, outside-bark calculations
may be unavailable.

Unitless.}
\item{\code{upper_ht1}, \code{upper_d1}}{A paired upper-stem measurement height
and diameter. Heights use feet or meters and diameters use inches
or centimeters. Both must be positive.

Three-point equations requiring the pair fail
when it is absent or incomplete. Some regional models use an upper
height alone under their own declared input rules.}
\item{\code{upper_ht2}, \code{upper_d2}}{Optional second measurement pair with
the same types, units, and positive domains.

Supply both where paired. Omission means no second measurement.}
\item{\code{upper_bark}}{Character bark basis for upper measurements,
\code{'ib'} or \code{'ob'}, defaulting to the model's convention. Flewelling upper measurements
default to inside bark.

Example:
\code{upper_bark = 'ib'}. A model may lack the conversion needed for
an outside-bark measurement. Unitless.}
\item{\code{form_class}}{Positive numeric form-class input for models
that declare it.

Unitless. Its definition
and any default belong to the selected equation. Missing a required
value prevents that model's calculation.}
\item{\code{site_index}}{Positive numeric site-index height, in feet or
meters, for models declaring it.

The model determines species and reference-age convention. Do not
assume that accepting this input means a particular call uses it.}
\item{\code{basal_area}}{Positive numeric stand basal area, in square feet
per acre or square meters per hectare. Available only where declared by the model.

Omitted optional inputs
use its defaults.}
\item{\code{decay_class}, \code{cull}}{Where declared by a taper model,
\code{decay_class} is a whole-number class from 1 through 5, and \code{cull}
is a numeric percentage from zero through 100. These are model auxiliaries, with
model-specific requirements. They do not replace \code{defects} or
\code{\link[=apply_defect_pct]{apply_defect_pct()}}.

The separate \code{\link[=biomass]{biomass()}} function
has its own live-tree and deduction conventions.}
}
}

\section{Status and missing values}{

With \code{status = TRUE}, \code{trees$status} and \code{logs$status} are integer columns. \code{ok} means
the calculation completed. \code{no_feasible_log} means a valid tree produced no feasible log.

Other failures require review before summarizing. A failed tree can retain partial output,
so its presence in a log table does not establish a complete result.

The merchandising codes are:
\describe{
\item{\code{no_entry_product}}{No product passed tree eligibility. Review species, diameter
classes, age, and pruning requirements.}
\item{\code{defect_height_out_of_range}}{A defect height is outside
the tree. Check its units and reference to ground.}
\item{\code{defect_interval_invalid}}{Defect bounds are invalid.

Check the interval and point-record conventions.}
\item{\code{defect_percent_out_of_range}}{A percentage is outside
zero through 100. Correct the recorded percentage.}
\item{\code{model_unresolved}}{No equation was resolved for the
species. Check the species and equation map.}
\item{\code{unknown_defect_effect}}{The effect is unsupported.

Use one of the documented operational treatments.}
\item{\code{unknown_curvature_category}}{A category is absent from
the ordered scale. Reconcile category labels.}
\item{\code{missing_product_attribute}}{A product needs a missing
tree attribute. Supply it or revise the product requirement.}
\item{\code{utilization_top_invalid}}{The requested top cannot be
resolved.

Check the diameter, bark basis, and equation.}
\item{\code{no_feasible_log}}{Valid zero volume under these
specifications. Retain the tree in inventory counts.}
\item{\code{scale_input_unavailable}}{A scaling measurement cannot be
calculated. Check species, bark information, and measurement inputs.

Missing scaled quantity and dependent value remain \code{NA}.
}
\item{\code{profile_adapter_failure}}{Profile evaluation or the
calculation plan failed. Inspect diagnostics and reproduce the tree
separately before treating the result as usable.}
\item{\code{reconciliation_failure}}{The volume accounting did not
close. Investigate the tree and model before reporting totals.}
}

Some invalid defect and
product inputs are caught earlier and stop the call instead of becoming
tree codes. Equation failures retain the input, model, capability, and evaluation diagnoses
returned by \code{\link[=status_codes]{status_codes()}}.

Before calculating totals, count statuses across the complete tree table. Do not silently drop
failed trees or interpret missing quantities as
zero. \code{\link[=product_summary]{product_summary()}} defaults to excluding missing quantities from
sums.

Use \code{na_action = 'propagate'} when an incomplete sum should remain
missing. A log-table summary alone cannot count trees having no logs.
}

\examples{
## Optimize cuts under the example prices
result <- optimize_bucking(dbh = example_trees$dbh,
                           ht = example_trees$ht,
                           model = example_trees$model,
                           species = example_trees$species,
                           products = example_products(name = 'douglas_fir'),
                           currency = 'USD',
                           objective = 'value',
                           status = TRUE)

## Draw the selected cuts
plot(result, tree = 5)
}
\seealso{
\code{\link[=merchandise]{merchandise()}} for longest-log selection, \code{\link[=compare_bucking_prices]{compare_bucking_prices()}}
for price scenarios, \link[=product_schema]{Product specification schema} for product fields,
\code{\link[=defect]{defect()}} for
restrictions, and \link[=result_tables]{Result tables} for returned columns.
}
