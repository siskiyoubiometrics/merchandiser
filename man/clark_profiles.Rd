% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem-clark.R
\name{clark_profiles}
\alias{clark_profiles}
\title{Choose inputs for Clark stem equations}
\description{
Clark models calculate stem dimensions from total height and optional upper-stem measurements.
\code{get_taper_model()} identifies the \code{clark_r8} or \code{clark_r9} implementation.
}
\details{
Applicable \code{clark_r8} equations accept \code{upper_ht1} for their identifier's top code.
Omission uses the source total-height path. \code{site_index} and \code{basal_area} are accepted
auxiliaries but are unused when total height is supplied.

Clark has no direct outside-bark profile or default bark ratio. \code{dob()} and
\code{height_at_dob()} require a supplied \code{bark_ratio}.

\code{stem_volume()} uses a geometric integral and the requested bark basis.
Source volume comparisons retain the source's separate total-volume and stump conventions.
For \verb{834CLKE110}, source compatibility reproduces the source inverse's addition of roots.
The default package mode returns the highest crossing and retains its diagnosis.
}
\examples{
## Inspect the Clark equation for the shipped southern species
get_taper_model(id = '831CLKE131')
}
\seealso{
\code{\link[=get_taper_model]{get_taper_model()}}, \code{\link[=dib]{dib()}}, \code{\link[=stem_volume]{stem_volume()}}, \code{\link[=height_at_dib]{height_at_dib()}}.
}
\keyword{internal}
