% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assumptions.R
\name{assumptions}
\alias{assumptions}
\title{Review the choices recorded with a result}
\usage{
## Call signatures
assumptions(x)
}
\arguments{
\item{x}{Required \code{merch_result} from \code{\link[=merchandise]{merchandise()}} or
\code{\link[=optimize_bucking]{optimize_bucking()}}, The function returns \code{run_metadata$assumptions} without validating its
contents. Other classes are errors. Missing recorded assumptions return \code{NULL}.}
}
\value{
A data frame with one row per distinct recorded choice. All fields
except \code{value} are unitless identifiers or descriptions. Character
\code{assumption} names the choice.

Integer \code{spcd} and character \code{species}
identify its species, and character \code{model} names the equation. Numeric \code{value} records a
magnitude in the character \code{units} field. Character \code{basis} identifies the bark or measurement
basis.

Character \code{product}, \code{preset}, and \code{source} identify the specification,
example table, and source. Integer \code{region}, \code{forest}, and \code{district}
record geographic selection codes. Missing fields mean not recorded or
not applicable to that choice.

For example, a stump choice can have \code{value = 1} and
\code{units = 'imperial'}, meaning feet for stump height.
}
\description{
Return the choices recorded in a merchandising result without repeating its calculations.
}
\details{
The table records equation mappings, automatically selected
example products, species-reference bark ratios, stump defaults, unit
system, and default utilization limits when applicable. It does not
contain local equation coefficients. Preserve those models separately.
}
\section{Status and missing values}{

No new calculation code is produced. Missing fields do not mean a zero
assumption or a failed tree. Inspect \code{x$trees$status} separately.

\code{no_feasible_log} there
denotes a valid result with no logs under the selected specifications.
}

\examples{
## Load dplyr
library(dplyr)

## Calculate logs for the shipped tree list
result <- merchandise(dbh = example_trees$dbh,
                      ht = example_trees$ht,
                      model = example_trees$model,
                      species = example_trees$species,
                      products = example_products(name = 'pnw'),
                      status = TRUE)

## Inspect the result
assumptions(x = result) \%>\%
  slice_head(n = 3)
}
\seealso{
\code{\link[=merchandise]{merchandise()}} to make a result, \code{\link[=taper_manifest]{taper_manifest()}} to record
available equations, \code{\link[=status_codes]{status_codes()}} to interpret failed trees.
}
